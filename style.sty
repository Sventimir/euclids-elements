\ProvidesPackage{style}

%% Style
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,
    urlcolor=cyan,
}

% Set standard fonts (Override scrartcl fontset).
\setkomafont{disposition}{\normalfont\bfseries}

\setlist{
    label=\arabic*.,
    noitemsep
}

\newlist{definitions}{enumerate}{1}
\newlist{postulates}{enumerate}{1}
\newlist{commons}{enumerate}{1}

\setlist[definitions]{
    label=\protect\hypertarget{defs-\arabic{part}.\arabic*}{\arabic*}.
}

\setlist[postulates]{
    label=\protect\hypertarget{posts-\arabic*}{\arabic*}.
}
\setlist[commons]{
    label=\protect\hypertarget{commons-\arabic*}{\arabic*}.
}

\renewcommand*\partformat{\thepart\autodot}

\renewcommand{\thepart}{Księga \Roman{part}}
\renewcommand{\thesection}{Twierdzenie \Roman{section}}

%% Commands

\newcommand{\qed}{\emph{q.e.d.}}

\newcommand{\proof}[1]{
    \textbf{Dowód}:

    #1 \qed
}

\newcommand{\defin}[1]{(\hyperlink{defs-#1}{definicja #1})}
\newcommand{\post}[1]{(\hyperlink{posts-#1}{postulat #1})}
\newcommand{\common}[1]{(\hyperlink{commons-#1}{o.z. #1})}
\newcommand{\prop}[1]{(twierdzenie #1)}

\renewcommand{\bold}[1]{\textbf{#1}}
